/**
 * The application-layer Clock port is the domain's minimal time source —
 * one interface, re-exported so use cases and adapters share the contract.
 */
export type { Clock } from "@domain/shared/Clock";
