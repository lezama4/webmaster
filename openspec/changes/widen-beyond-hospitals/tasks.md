# Tasks: widen-beyond-hospitals

**Depends on:** `bootstrap-vivetutiempo-platform` (deployed) and `hospital-finder-and-home-clarity` (D9–D15, deployed). **Phase 1 (model + migration, D16/D17) MUST land and be verified against real Postgres before ANY later phase starts** — every other phase reads the `CENTRE` role/profile-type and the `CentreType` axis this phase creates. Chain strategy: `feature-branch-chain` — PR1 targets the tracker/feature branch; each later PR targets the immediately preceding PR's branch, so only the tracker merges to `main`.

**Strict TDD is ACTIVE** for every `domain/` and `application/` task (Phases 1–3). `infrastructure/`/`ui/` stay pragmatic — tests still required, follow the code, per project convention. Integration tests are silently SKIPPED unless run with `VIVETUTIEMPO_RUN_INTEGRATION=true` — a skipped integration test is NEVER reported as passed. **Every integration test run WIPES the seed (`resetDatabase()` per test, shared Neon `dev` branch) — `npm run db:seed` MUST be re-run immediately after, before any e2e run or manual demo, in every phase below that runs integration tests.** Node is not on default PATH: prefix commands with `export PATH="/c/Program Files/nodejs:$PATH"`. npm pinned at 11.18.0 — never bare `npm install`.

**Naming constraint carried through every phase below:** `PublicHospitalProjection`, `PublicHospitalDirectoryQuery`, `listPublicHospitals`, `hospitalDirectoryDeps`, the route `/api/hospitals`, and the slug `/encuentra-tu-momento` **keep their existing names**. This is a deliberate deferral (proposal §5.4, design D19 "Rename deferral (3b)"), not an oversight — `/api/hospitals` and `/encuentra-tu-momento` are shipped, and PR #11's Open Graph/share links depend on the slug. Any task below that touches these names keeps them exactly as they are.

## Phase 0: Pre-Flight — state check, not work

- [x] 0.1 Confirm Block 3 (simulated payments, `feat/support-payments`, 6 local unpushed commits, reviewed to ESCALATED per proposal §10/R10) is either merged to `main` or explicitly parked before this change's Phase 1 branches off. Re-confirm the proposal's exploration finding: no payment code under `src/` keys anything by `AccountRole`/`ProfileType`/`CentreType`. If Block 3 has since introduced such a dependency, STOP and re-assess R10 before continuing. — CONFIRMED: `git log` shows Block 3 already merged to `main` (PR #12). Grep confirms no `src/` payment code keys anything by `AccountRole`/`ProfileType`/`CentreType`.
- [x] 0.2 Confirm the working tree is clean on the branch this change starts from (`git status`); this is the tracker branch PR1 will target under `feature-branch-chain`. — CONFIRMED clean on `feat/widen-centres-domain` before starting.

## Phase 1: Domain Model + Migration (D16, D17) — strict TDD, HIGHEST RISK unit — PR1

**This is the highest-risk unit in the whole change.** The migration MUST be hand-written raw SQL — Prisma's declarative diff does NOT infer `RENAME VALUE`; editing `HOSPITAL` → `CENTRE` in `schema.prisma` and running `prisma migrate dev` generates a destructive DROP-and-recreate that would fail or orphan rows. Do not let tooling auto-generate this migration.

- [x] 1.1 [RED] `tests/unit/domain/profile.test.ts` (extend, EXISTING file): assert the new invariant — `type === "centre"` requires a valid `centreType` (one of the six values); `type === "artist"` forbids `centreType`; `assertValidCentreType` rejects an unknown string (e.g. `"prison"`). Covers centre-registration spec "centreType is constrained to the six known values" and "An artist registration forbids centreType" at the domain layer.
- [x] 1.2 [GREEN] `src/domain/profile/Profile.ts`: rename `ProfileType` value `"hospital"` → `"centre"`; add `CentreType = "hospital" | "nursing_home" | "day_centre" | "day_hospital" | "occupational_centre" | "palliative_unit"` + `CENTRE_TYPES` array + `assertValidCentreType`; `Profile.centreType?: CentreType`; `CreateProfileInput`/`RehydrateProfileInput` gain `centreType?`; enforce the type/centreType biconditional in the factories. Makes 1.1 pass.
- [x] 1.3 [RED] `tests/unit/domain/account.test.ts` (extend, EXISTING file, if the rename affects any assertion there): confirm `AccountRole` includes `"centre"` (not `"hospital"`) and `profileTypeForRole("centre") === "centre"`, `profileTypeForRole("artist") === "artist"` — the 1:1 map survives the rename unchanged in shape (D16 §"the 1:1 relation was never the obstacle").
- [x] 1.4 [GREEN] `src/domain/account/Account.ts`: rename `AccountRole` value `"hospital"` → `"centre"`; update `ACCOUNT_ROLES`; `profileTypeForRole` keeps its 1:1 shape with the renamed value.
- [x] 1.5 [GREEN, mechanical] `src/infrastructure/persistence/prisma/mappers.ts`: rename `HOSPITAL` → `CENTRE` in `ACCOUNT_ROLE_TO_*`/`PROFILE_TYPE_TO_*` map pairs; add `CENTRE_TYPE_TO_DOMAIN`/`CENTRE_TYPE_TO_PRISMA`; `ProfileRow` gains `centreType?: PrismaCentreType | null`; `toDomainProfile` maps it (present only for centres, `null` for artists). No test-file rename needed — existing mapper tests extend in place. Also updated `ProfileRepository.ts` (save/select `centreType`) — required for the mapper's round-trip to actually work; not explicitly listed above but necessary wiring.
- [x] 1.6 `prisma/schema.prisma` (edit, target end-state): rename `AccountRole.HOSPITAL` → `CENTRE`; rename `ProfileType.HOSPITAL` → `CENTRE`; add `enum CentreType { HOSPITAL NURSING_HOME DAY_CENTRE DAY_HOSPITAL OCCUPATIONAL_CENTRE PALLIATIVE_UNIT }`; add `Profile.centreType CentreType?`. Edited so Prisma sees no drift once the hand-written migration below is applied — do NOT run `prisma migrate dev` against this edit.
- [x] 1.7 **Hand-written migration** `prisma/migrations/20260723000000_widen_centre_types/migration.sql` (NEW, raw SQL, authored by hand exactly as the D4 partial-unique-index migration was):
  ```sql
  ALTER TYPE "AccountRole" RENAME VALUE 'HOSPITAL' TO 'CENTRE';
  ALTER TYPE "ProfileType" RENAME VALUE 'HOSPITAL' TO 'CENTRE';

  CREATE TYPE "CentreType" AS ENUM (
    'HOSPITAL','NURSING_HOME','DAY_CENTRE','DAY_HOSPITAL',
    'OCCUPATIONAL_CENTRE','PALLIATIVE_UNIT'
  );

  ALTER TABLE "profiles" ADD COLUMN "centreType" "CentreType";

  UPDATE "profiles" SET "centreType" = 'HOSPITAL' WHERE "type" = 'CENTRE';
  ```
  Document the down path inline as a SQL comment (not a separate down-migration file — Prisma doesn't run one automatically): reverse order is `UPDATE profiles SET centreType = NULL` → `ALTER TABLE profiles DROP COLUMN centreType` → `DROP TYPE "CentreType"` → `ALTER TYPE ... RENAME VALUE 'CENTRE' TO 'HOSPITAL'` on both enums; state explicitly in the comment that this is lossless ONLY while no non-hospital `centreType` row exists (migration-safety spec, "Down Migration Is Documented and Honest About Its Limit").
- [x] 1.8 [Test, before trusting 1.7] Confirm the D4 partial-unique-index migration (`prisma/migrations/20260711000001_partial_unique_indexes/migration.sql`) references `ProposalStatus` only, NOT `AccountRole`/`ProfileType` — re-verify by reading the file directly (not from memory), so this task's own record confirms D4 is unaffected before the new migration runs. — CONFIRMED by reading the file directly: only `'ACCEPTED'::"ProposalStatus"` / `'SUBMITTED'::"ProposalStatus"`, no reference to `AccountRole`/`ProfileType`.
- [x] 1.9 [Integration test, `VIVETUTIEMPO_RUN_INTEGRATION=true`, REQUIRED, run against REAL Postgres — never mocked] `tests/integration/centre-migration.test.ts` (NEW), covering every migration-safety spec scenario:
  - an existing `HOSPITAL` row reads `type: CENTRE` + `centreType: "hospital"` post-migration, every other field (name, city, postalCode, addressLine, latitude, longitude, status, account credentials) byte-identical to its pre-migration value;
  - every Slot/Event previously associated with a pre-migration hospital profile is still associated with it, same count, no orphans;
  - the Account authenticates with the same credentials and `status: ACTIVE` post-migration;
  - a pre-migration Artist profile is untouched, `centreType` is `NULL`;
  - `profiles`/`slots`/`events` row counts are unchanged before/after;
  - D4's two partial unique indexes (`ACCEPTED` singleton per slot, `SUBMITTED` singleton per slot+artist) still enforce correctly after the migration (proves 1.8's claim at runtime, not just by reading the file).
  **Run with the seed loaded** (`npm run db:seed` before this test file, since `resetDatabase()` inside the test itself wipes it again per-test — the test file's own fixtures are what get migrated, not the demo seed). Re-run `npm run db:seed` immediately after this task's test run, before any later manual verification. — RAN against real Neon `dev` branch (`ep-snowy-sunset-abmfm9fs`): 673/673 tests passed (0 skipped, 0 failed), migration `20260723000000_widen_centre_types` applied cleanly. `npm run db:seed` re-run immediately after.
- [x] 1.10 [Rollback verification] Manually or via a throwaway script, apply the down path documented in 1.7 against a scratch Postgres state with (a) no non-hospital centre present — confirm clean full reversal — and (b) one `palliative_unit` row present — confirm it documents the coarsening (reverts to `HOSPITAL`, drops `centreType`) rather than crashing or silently losing the row. Record both outcomes in this task's own note; do not merely assert the SQL comment is correct without running it once. — RAN via a throwaway Node script against the real dev DB, each scenario wrapped in a Prisma interactive transaction that was deliberately rolled back (never committed) so the shared branch was left untouched. (a) 0 non-hospital rows present → down path applied cleanly, `AccountRole`/`ProfileType` labels reverted to `HOSPITAL`, all sample rows read `type: HOSPITAL` — clean full reversal confirmed. (b) one inserted `PALLIATIVE_UNIT` row → down path applied without error; the row survived but its `type` coarsened to `HOSPITAL` and its `centreType` was dropped with the column — confirmed as a documented coarsening, not a crash or silent row loss. Post-verification confirmed zero permanent effect: `CENTRE`/`CentreType` still present in the real DB afterward, no leftover rollback-test rows. Script deleted after use (not committed).
- [x] 1.11 Verify `npx tsc --noEmit` and `npm run lint` are clean after 1.2/1.4/1.5/1.6 — the enum rename touches every file importing the old `"hospital"` literal at the type level; a compile error here is expected to surface every call site Phase 2 must then update. — Both clean across the WHOLE repo (not just Phase 1 files). The enum rename's compile errors, as expected, surfaced ~28 files (mostly test fixtures/builders and the ~8 guard call sites design assigns to PR2/task 2.4); fixed all of them as minimal literal/mechanical renames in this batch so `npm run test` stays at the 594-passed baseline (now 604 with new domain tests added) rather than leaving PR1 in a broken-build state. `registerProfile.ts` and the guard call sites were touched ONLY as much as needed to compile (a hardcoded `centreType: "hospital"` stopgap in `registerProfile.ts`, clearly commented as a PR2 replacement point) — no PR2 semantic work (dynamic input field, UI, admin display) was added.

## Phase 2: Registration Redesign + Guards (D18) — strict TDD (application), pragmatic (UI) — PR2 (base: PR1 branch)

- [x] 2.1 [RED] `tests/unit/application/registerProfile.test.ts` (extend, EXISTING file): centre registration requires `centreType` (rejected before profile creation if absent, independent of client-side validation — centre-registration spec "centreType is required for a centre role"); `centreType` constrained to the six values via `assertValidCentreType` (spec "centreType is constrained to the six known values"); artist registration with a `centreType` present is rejected (spec "An artist registration forbids centreType"); each of the six types independently produces a `PENDING` profile with the submitted `centreType` persisted unchanged (spec "Each of the six centre types can register"). — DONE: added a `describe("centreType (D18) ...")` block with an `it.each` over the six types plus the three rejection scenarios; also fixed 4 pre-existing tests that relied on the removed hardcode to pass `centreType: "hospital"` explicitly.
- [x] 2.2 [GREEN] `src/application/use-cases/registerProfile.ts`: `RegisterProfileInput` gains a required-when-`centre` `centreType: CentreType` field; `hospitalLocationFrom` → `centreLocationFrom`, gated on `role === "centre"` (was `"hospital"`), same field-picking behaviour; server-side validation does not trust the form. — DONE: removed the PR1 `centreTypeFrom(type)` hardcode stopgap; new `centreTypeFrom(input)` passes `input.centreType` straight through for ANY role (deliberately not gated like location), so `createProfile`'s domain invariant is what rejects an artist-with-centreType, not a silent drop.
- [x] 2.3 `src/app/api/auth/register/route.ts` (edit): parse and validate `centreType` — required when `role === "centre"`, must be one of `CENTRE_TYPES`, rejected before reaching the use case if malformed. — DONE: `optionalCentreType()` reuses domain `assertValidCentreType` for the format guard (rejected before `registerProfile` runs); "required when centre" is deliberately left to the domain invariant (matches the existing thin-route/"does not trust the form" convention — `role` itself is handled the same way).
- [x] 2.4 [RED→GREEN, mechanical, one commit] Guard literal swap at the ~8 call sites — `src/application/use-cases/publishSlot.ts`, `registerProfile.ts`, `rejectProposal.ts`, `closeSlot.ts`, `listHospitalSlots.ts`, `approveProposal.ts` (and any others `guards.ts`'s `assertRole`/`assertActiveProfile` literal `"hospital"` appears in): rename literal `"hospital"` → `"centre"`. No new argument, no branch on `centreType`. — CONFIRMED already done in PR1's compile-driven fix pass (task 1.11); re-verified by grep across all call sites in this batch — all read `"centre"`, none branch on `centreType`.
- [x] 2.5 [RED] `tests/unit/application/guards.test.ts` (extend, EXISTING file, or add if it does not exist as a dedicated file — confirm by reading the current test tree first): two ACTIVE centre profiles with different `centreType` values are authorised by the identical guard check (centre-registration spec "Guard authorises regardless of centreType"); a request context with an arbitrary/unknown `centreType` string does not affect the authorization outcome of `assertRole`/`assertActiveProfile` (spec "An unknown or manipulated centreType cannot affect authorization outcome"). — DONE: confirmed no dedicated file existed; created `tests/unit/application/guards.test.ts` with 5 tests covering all six centreType values, a manipulated-Actor-shaped-object case, and a denial-unaffected case.
- [x] 2.6 [GREEN] Confirm 2.5 passes against the guards as swapped in 2.4 — this is a regression/invariant guard, not new production logic; if it fails, the guard has grown a `centreType` branch it must not have. — CONFIRMED: all 5 pass unmodified against the existing `guards.ts`.
- [x] 2.7 `src/app/register/page.tsx` (edit): role select option renamed `Register.role.hospital` → `Register.role.centre`; when role is `centre`, a **required** `centreType` `<select>` appears (six options) replacing the hidden hospital default; existing optional location fields now shown for any centre role, not just hospitals. — DONE, with one deliberate deviation: the `Register.role.hospital`/`.artist` i18n KEYS are kept as-is (not renamed) per the orchestrator's explicit PR2 scope ("just the six type labels + the field label" — narrative/label-text rewrite is PR6); only the DOM `<option value>` changed from `"hospital"` to `"centre"` (a functional necessity — `AccountRole` no longer accepts `"hospital"` at all, so the pre-PR2 form was silently broken for centre registration). Local `CentreType`/`CENTRE_TYPE_OPTIONS` mirror the domain union (matches `PublishSlotForm`'s `AUDIENCE_OPTIONS` convention) rather than importing `@domain` into a client component.
- [x] 2.8 Admin pending-profile surface (edit): the admin pending-profile query/DTO gains `centreType`; `src/app/admin/profiles/page.tsx` displays the specific kind (e.g. "Residencia de mayores") via a new `AdminProfiles.centreType.*` label group, not a flat "Centro". — DONE across the full allow-list chain: `PendingProfileView` DTO, `listPendingProfiles.ts`'s field-by-field rebuild (D6 hostile-adapter discipline — `centreType` had to be added to the rebuild or it would be silently stripped), `PrismaPendingProfileQuery.ts` (select + map via `toDomainCentreType`), and the page itself. Also renamed the pre-existing `AdminProfiles.types.hospital` key to `.centre` (mechanical — `profile.type` reads `"centre"` now, so the old key was already a dead lookup) — text kept as "Hospital"/"Hospital"/"Ospitalea" unchanged, narrative deferred to PR6.
- [x] 2.9 [Integration test, `VIVETUTIEMPO_RUN_INTEGRATION=true`] `tests/integration/centre-lifecycle.test.ts` (NEW): all six `centreType` values register → an Admin approves the PENDING profile through the existing validation flow (unchanged in shape, centre-registration spec "Admin validates a centre of a given type") → the now-ACTIVE profile publishes a Slot through the identical guard path used before this change. This is the integration test that proves D16's gradable claim ("the seventh centre type is a new row of data, not a new branch of code") at runtime. Re-run `npm run db:seed` after this test (it wipes the shared dev DB via `resetDatabase()`). — DONE, and this test caught a REAL pre-existing bug (see Issues Found in apply-progress): `PrismaRegistrationUnitOfWork`'s own `profileData()` serializer never mapped `centreType` at all, so every centre ever registered through `registerProfile` silently persisted a NULL `centreType` to Postgres (only the in-memory domain object had it). Fixed in this batch (see `src/infrastructure/persistence/prisma/RegistrationUnitOfWork.ts`). Ran against real Neon `dev`: 696/696 passed (0 skipped, 0 failed). `npm run db:seed` re-run immediately after.
- [x] 2.10 [Test] Pre-migration hospital account unaffected: extend 2.9 or a dedicated case — an Account/Profile seeded as pre-migration `HOSPITAL` (i.e. read post-migration as `centre`/`centreType: hospital`) authenticates and performs every centre-only action with zero behavioural change (centre-registration spec "Pre-migration hospital account is unaffected"). — DONE as a dedicated case in `centre-lifecycle.test.ts`, reusing the `createHospitalProfile` fixture (the same pre-migration shape `centre-migration.test.ts`/Phase 1.9 already proved the migration produces): publishes AND closes a Slot through the identical guard path. Account-credential authentication itself was already proven post-migration by Phase 1.9's `centre-migration.test.ts`; this task adds the actions half.
- [x] 2.11 Verify `npx tsc --noEmit` and `npm run lint` clean after this phase. — CONFIRMED clean across the whole repo.

## Phase 3: Public Directory — Application Layer + D10 Re-Run (D19a) — strict TDD — PR3 (base: PR2 branch)

**Correction from design, honoured here — do not deviate:** `type` (the `ProfileType` role field) stays **forbidden** on the public allow-list. Only `centreType` is newly admitted. The security predicate stays `type: "CENTRE"` (one literal) — it does **not** become a six-value `centreType IN (...)` list.

- [x] 3.1 [RED] `tests/unit/application/listPublicHospitals.test.ts` (extend, EXISTING file): exact-key-set assertion extended by hand to `["centreType","city","latitude","longitude","name","postalCode"]` (six keys, public-hospital-directory spec "Response contains exactly the six allow-listed keys"); a new hostile-adapter case injects `type` alongside `addressLine`/email/id/event-derived fields — asserts `type` is stripped while `centreType` passes through (spec "Hostile adapter injecting type or event-derived fields is stripped"); a pre-existing-hospital-row case asserts `centreType: "hospital"` appears (spec "A pre-existing hospital row shows centreType hospital"). — DONE, confirmed RED before implementing (8 failures).
- [x] 3.2 [GREEN] `src/application/dto/PublicHospitalProjection.ts`: add `readonly centreType: CentreType;` to the allow-list; update the D14 `ForbiddenPublicHospitalKey`/`AssertNever` compile-time guard so `type` remains listed as forbidden and `centreType` is the new allow-listed member (public-hospital-directory spec "PublicHospitalProjection Is an Exact Allow-List"). Update the DTO's doc comment to name ADR D19 and state "hospital" in this file now means "any care centre". — DONE. Sabotage-verify performed: temporarily added `readonly type: string` to the interface — `tsc` failed immediately with `TS2344: Type '"type"' does not satisfy the constraint 'never'` on the `_NoForbiddenFields` guard (plus cascading errors) — then reverted.
- [x] 3.3 [GREEN] `src/application/use-cases/listPublicHospitals.ts`: the field-by-field DTO rebuild gains `centreType`. No `Actor`, no authorization — unchanged, anonymous by design. — DONE.
- [x] 3.4 [RED→GREEN] `tests/unit/application/nonCorrelation.test.ts` (extend, EXISTING dedicated D10 owner file, do NOT create a second file): `HOSPITAL_ALLOW_LISTED_FIELDS` gains `centreType` **by hand**, named to ADR D19 in a comment (design: "gain `centreType` by hand, never derived"); assert `centreType` is present on the directory side AND that no Slot/Proposal/Event-derived field accompanies it; both directions re-asserted (public-event-browsing spec "Non-correlation suites pass after centreType is added"). — DONE.
- [x] 3.5 [RED→GREEN] Extend the existing `listPublishedEvents`/`PublicEventProjection` unit test coverage (or `nonCorrelation.test.ts` if that is the established home for hostile-adapter cross-domain cases — confirm by reading the current file before choosing): a hostile adapter injecting `centreType`, a centre `id`, `name`, `city`, `postalCode`, and coordinates onto an Event is stripped entirely (public-event-browsing spec "Hostile adapter cannot attach centreType or centre identity to a public Event"); confirm the Events DTO itself is unmodified (public-event-browsing spec "Public Event surface still has no location or centre-identifying field"). — DONE, in `nonCorrelation.test.ts` (confirmed established home): `HOSPITAL_IDENTIFYING_FIELDS` gained `centreType`, the existing hostile-adapter Event test extended to inject `centreType: "palliative_unit"` alongside the pre-existing hospital-identity fields. `PublicEventProjection` itself untouched.
- [x] 3.6 Confirm the ESLint D10 boundary zones (`eslint.config.mjs`, from `hospital-finder-and-home-clarity` Phase 2.4) still pass unmodified — this change adds no new cross-import path between the two surfaces, so no zone edit is expected; run the existing `tests/unit/lint/eslint-boundary.test.ts` to confirm. — CONFIRMED: 19/19 passed unmodified.
- [x] 3.7 (unplanned, compile-necessitated) Making `centreType` a REQUIRED (non-null) field on `PublicHospitalProjection` per D19/design ("non-null") cascaded a compile break to 4 files outside this phase's nominal scope, exactly the same shape as Phase 1.11's cascade — fixed MECHANICALLY (object-literal field additions / a select+map wiring), no semantic PR4/PR5 work added: `src/infrastructure/persistence/prisma/PublicHospitalDirectoryQuery.ts` (select gains `centreType: true`, rebuild maps it via the existing `toDomainCentreType` mapper, with a defensive throw if a CENTRE row ever has a null `centreType` — a D16 invariant violation, not an expected runtime path); `tests/unit/application/fakeContracts.test.ts`, `tests/unit/ui/filterHospitals.test.ts`, `tests/unit/ui/selectMappableHospitals.test.ts` (fixture literals gain `centreType: "hospital"`). PR4 still owns: seed diversification, the six-type integration test extension, and the e2e location-tuple export — none of that was added here.

## Phase 4: Public Directory — Infrastructure + Seed (D19b) — pragmatic tests — PR4 (base: PR3 branch) — DONE, all 5 tasks [x] (4.1–4.5)

Committed on `feat/widen-centres-infra` (base: PR3's `feat/widen-centres-directory-app`), NOT pushed, NOT PR'd, per instructions.

- [x] 4.1 `src/infrastructure/persistence/prisma/PublicHospitalDirectoryQuery.ts` (edit): `select` gains `centreType: true`; security predicate `where: { type: "HOSPITAL", status: "ACTIVE" }` → `where: { type: "CENTRE", status: "ACTIVE" }`; the field-by-field DTO rebuild gains `centreType`. — CONFIRMED already done, not re-implemented: PR3's task 3.7 (unplanned compile-cascade fix) had already wired this exact select/predicate/rebuild while making `centreType` non-null on the DTO. Re-verified by reading the file directly this batch, not assumed.
- [x] 4.2 `prisma/seed.ts` (edit) — **DEVIATION from this task's original 5-new-row table, decided and recorded here:** rather than adding five brand-new rows, FOUR of the ten existing roster hospitals (Urumea/Monteverde/Besòs/Bernesga) were repurposed in place — same fixed id/city/postalCode/addressLine/coordinates, only `name`+`centreType` changed (`nursing_home`/`day_centre`/`day_hospital`/`palliative_unit` respectively) — because they were confirmed (by grep, not assumed) to be free of any name-dependent assertion elsewhere. San Juan, del Mar, Santa Clara, San Rafael, do Orzán and del Guadiana were deliberately left untouched/hospital: their exact NAME backs other e2e assertions (name/city/diacritic search, city-sort-first, the no-coordinates case) that this phase's scope does not own re-writing. ONE brand-new ACTIVE row was added for the sixth type this repurposing couldn't safely reach — Centro Ocupacional Aravaca (`occupational_centre`, Madrid, 28023, new fixed id/account/email), matching the design table's Aravaca entry exactly. Esperanza stays PENDING, byte-identical, never touched. Net: 11 ACTIVE centres (was 10), all six `CentreType` values represented — confirmed by a direct DB query after seeding: `HOSPITAL:6, DAY_HOSPITAL:1, NURSING_HOME:1, PALLIATIVE_UNIT:1, OCCUPATIONAL_CENTRE:1, DAY_CENTRE:1`.
- [x] 4.3 `e2e/support/helpers.ts` (extend): `SEED_ACTIVE_HOSPITALS` tuples gain a `centreType` field per row (reflecting the 4 repurposed + 1 new row above); new `SEED_ACTIVE_CENTRE_TYPES` export (all six values, for PR5's filter e2e); `SEED_HOSPITAL_LOCATIONS` gains Aravaca's `addressLine`.
- [x] 4.4 [Integration test, `VIVETUTIEMPO_RUN_INTEGRATION=true`] `tests/integration/public-hospital-directory-query.test.ts` (extended, EXISTING file): `SeedHospitalInput`/`seedHospital()` gained an optional `centreType` (default `"hospital"`, preserves every pre-existing call site unchanged); the "5 allow-listed keys" test corrected to 6 (`centreType` present, asserted `"hospital"`, `type` absent from the raw JSON — this test was stale since PR3 widened the DTO, now fixed); NEW test seeds all six `CENTRE_TYPES` as distinct ACTIVE rows plus a PENDING `palliative_unit` row, asserts all six `centreType` values are returned and the PENDING one is absent (public-hospital-directory spec "Seed produces all six centre types, demonstrably filterable"); pre-existing idempotency test (4.3's original number) unchanged, still last in the file. Ran against real Neon `dev`: 698/698 passed (0 skipped, 0 failed). `npm run db:seed` re-run immediately after; distribution confirmed by direct query (see 4.2).
- [x] 4.5 `src/infrastructure/composition/container.ts`: confirmed `hospitalDirectoryDeps()` needs no shape change — read the file directly; it wires the identical `PrismaPublicHospitalDirectoryQuery`/`ListPublicHospitalsDeps` pair, now returning one more field through the same port. No edit made.

**Also fixed as necessary e2e infrastructure (not new scope, keeping the existing suite green against the already-widened DTO):** `e2e/hospital-directory.spec.ts`'s `ALLOWED_HOSPITAL_KEYS` and `e2e/non-correlation.spec.ts`'s `ALLOWED_HOSPITAL_KEYS` were both stale at 5 keys since PR3 (`centreType` already shipped, unassert-ed there) — both corrected to the 6-key set; `hospital-directory.spec.ts`'s allow-list test also gained a per-row `centreType` equality check against `SEED_ACTIVE_HOSPITALS`. Not run this batch (e2e requires a running server + `npm run test:e2e`, out of this batch's verification scope; static correctness confirmed by `tsc`/lint only).

Verification: `npx tsc --noEmit` clean repo-wide; `npm run lint` clean; `npm run test` → 621 passed/77 skipped (baseline 621/76 — net +1 skipped, the new integration test, correctly gated behind `VIVETUTIEMPO_RUN_INTEGRATION`, no regression); `VIVETUTIEMPO_RUN_INTEGRATION=true npm run test` → 698/698 passed (0 skipped, 0 failed) including the new/extended integration tests; `npm run db:seed` re-run after.

Next: PR5 (Phase 5 — client-side filter + Finder UI + e2e) NOT started.

## Phase 5: Client-Side Filter + Finder UI + E2E (D12 extension, D19 UI) — pragmatic tests — PR5 (base: PR4 branch)

- [ ] 5.1 [RED] `tests/unit/ui/filterHospitals.test.ts` (extend, EXISTING file): a `centreType` predicate alone narrows to matching rows; combined with the existing text-query predicate by AND (public-hospital-directory spec "Type filter combines with text search"); an "all types" passthrough case; deterministic order preserved.
- [ ] 5.2 [GREEN] `src/ui/finder/filterHospitals.ts` (or the sibling pure fn per the existing pattern): add the `centreType` predicate, AND-combined with the existing text predicate.
- [ ] 5.3 `src/app/encuentra-tu-momento/HospitalFinder.tsx` (edit): add a small set of accessible filter controls (one per `centreType` + "all"); reflect selection to the URL as `?type=` alongside the existing `?q=`; the visibly-displayed type tag per result (public-hospital-directory spec "The directory shows the type"); result-count `aria-live` region continues to announce the filtered count (D11, unchanged mechanism).
- [ ] 5.4 `src/app/encuentra-tu-momento/page.tsx` (edit, minimal): read `type` alongside `q` from `searchParams` (Next 16 Promise pattern, matching the existing `q` handling) and pass it through as the `HospitalFinder`'s initial filter state.
- [ ] 5.5 [E2E, extend] `e2e/hospital-directory.spec.ts` (EXISTING file): a family selects the "Residencia de mayores" filter — only `nursing_home` rows remain, URL reflects `?type=nursing_home` (public-hospital-directory spec "A family filters by residencia"); type filter combined with a text search narrows to the intersection; directory renders all six kinds across the widened seed; each result's `centreType` tag is visible in the DOM.
- [ ] 5.6 [E2E, re-run + extend, D10] `e2e/non-correlation.spec.ts` (EXISTING file): re-run unchanged in intent against the widened directory (public-event-browsing spec "Non-correlation suites pass after centreType is added"); extend with a case for a city with exactly one ACTIVE `palliative_unit` and no other centre in that city — confirm no Event field reveals a location or originating centre (spec "A lone centre of a rare type does not narrow any event"); confirm events from centres of two different `centreType` values remain indistinguishable by centre (spec "Events from centres of different types remain indistinguishable by centre").
- [ ] 5.7 Verify `npm run test:e2e` for this file set green; re-run `npm run db:seed` if any integration test in Phase 4 was re-run in the same session before this task.

## Phase 6: Vocabulary — Three Languages (D20) — mechanical labels, then narrative rewrite, then Basque gate — PR6 (base: PR5 branch)

**Sequenced deliberately after Phases 2–5** so this phase's `messages/*.json` edits do not re-conflict with the label additions those phases' UI tasks already introduced (design: "Touches the same JSON as 2 and 3 — sequence it after them"). Split into two work units — mechanical first, narrative second — per instruction: they are different kinds of work with different risk profiles.

### 6a — Mechanical label additions (low judgement, structural)

- [ ] 6.1 [RED] `tests/unit/i18n/localeParity.test.ts` (extend, EXISTING file): flattened key-set parity check extended to cover the new `CentreType.*`, `Register.centreType.*`, `AdminProfiles.centreType.*` namespaces (i18n-locale-parity spec "All three locale files are in parity after the vocabulary rewrite"); a targeted assertion that exactly six `CentreType.*` keys exist per locale (spec "Exactly six CentreType labels exist per locale"); a case proving a key present in `es` but missing from `eu`/`en` fails and names the missing key + locale (spec "A new CentreType key missing from one locale fails the check").
- [ ] 6.2 `messages/es.json` + `messages/en.json` (edit, same commit): add the six `CentreType.*` display labels (hospital/nursing_home/day_centre/day_hospital/occupational_centre/palliative_unit), `Register.role.centre` (replacing `Register.role.hospital`), `Register.centreType.*` (six option labels + field label), `AdminProfiles.centreType.*` (six labels for the admin queue display), and the generic `Finder` title/nav copy (design D20 table: "Encuentra tu centro" es / analogous en — the finder title no longer reads "Encuentra tu hospital"; public-information spec "Finder title is generic"). ICU placeholder parity preserved for any templated key.
- [ ] 6.3 `messages/eu.json` (edit): **DRAFT** Basque translations for the same key set added in 6.2. Genuine best-effort translation, no empty strings, no placeholder/stub text — but explicitly UNREVIEWED (flagged again at 6.7).
- [ ] 6.4 [GREEN] Confirm 6.1 passes after 6.2/6.3 land — key-set + ICU-placeholder parity across all three locales, `CentreType` namespace has exactly six keys per locale.

### 6b — Narrative rewrite (semantic, judgement-heavy — not a substitution)

- [ ] 6.5 `messages/es.json` + `messages/en.json` (edit, same commit): rewrite the narrative keys off the hospital premise, per the D20 table — `Home.mission`, `Home.trust.title`, `Home.trust.hospitalsLabel` (→ "Centros participantes"/"Participating centres"), `About.title`, `About.purpose.description`, `About.roles[].name` ("Hospital" → "Centro de cuidado"/"Care centre"), `Layout.footer.description`, `Help.howItWorks`, `Help.steps.*`. Relational phrasing where the type is unknown ("las personas que están allí" / "the people who are there"); type-specific terms only where the UI already knows the type (registration, slot board, admin queue, directory tag — already wired in Phases 2/5). `/quienes-somos` (`About` namespace) states the generic centre role, not "Hospital" (public-information spec "The generic role is named, not Hospital"), and its data-stance section states that `centreType` is published alongside name/city/postal/coordinates, and that street address, the internal `type` field, and cross-surface correlation are not (spec "Data stance mentions centreType as published").
- [ ] 6.6 `messages/eu.json` (edit): **DRAFT** Basque narrative rewrite, drafted independently per the same relational-phrasing principle — NOT derived/translated from the Spanish (design D20: "the lexical gap exists separately in each"). Genuine best-effort, no stubs — still explicitly UNREVIEWED.
- [ ] 6.7 [Test] Re-run `tests/unit/i18n/localeParity.test.ts` — confirm still green after the narrative keys land in all three files (structure/ICU parity only).
- [ ] 6.8 **MANUAL REVIEW TASK — non-automatable, not a test task.** Spec scenario (public-information, manual review): "Home mission reads correctly for a residencia" — a human reviewer reads `Home.mission`, `Home.trust`, `About.purpose` from the premise of a nursing-home resident or day-centre participant, confirms no sentence presupposes a hospital ward/bed/"estancia hospitalaria" as the only setting. Record the review outcome explicitly (pass/fail + any follow-up edit), not merely the agent's own self-assessment.
- [ ] 6.9 **MANUAL REVIEW TASK — non-automatable, not a test task.** Spec scenario (public-information, manual review): "Help and footer copy do not assume paciente as the only person served" — a human reviewer checks `Help.howItWorks`, `Help.steps.*`, `Layout.footer.description` against all six centre types, confirms relational phrasing or (where type-known) the correct type-specific term is used, never "paciente" as an assumed universal. Record the outcome explicitly.
- [ ] 6.10 **BLOCKING GATE — CANNOT be closed by the implementing agent.** Every `eu` string touched in 6.3 and 6.6 (the six `CentreType` labels, the type-specific person terms, every rewritten narrative key) is DRAFT and requires review and sign-off by the assigned native Basque speaker before merge (i18n-locale-parity spec "Basque Vocabulary Quality Is a Blocking Open Gate, Not a Passing Assertion"). A passing `localeParity` run (6.7) is structure-only and MUST NOT be reported or cited as Basque quality assurance — report it explicitly as "structural parity: pass, translation quality: pending native review." Surface this as an open item to the user; the change is not merge-ready until resolved. Same convention as `hospital-finder-and-home-clarity` task 13.5.

## Phase 7: TFM Documentation — PR7 (base: PR6 branch)

**In scope**, per explicit instruction: `README.md`, `docs/memoria-tfm-borrador.md`, `docs/slides-outline.md`, `docs/security-threat-model.md`, `docs/tfm-readiness-report.md`. **`docs/video-script.md` is deliberately EXCLUDED** — the user is deferring the video; no task updates it in this change.

- [ ] 7.1 `README.md` (edit): describe the generalised six-`centreType` product (not hospital-only); document the new registration flow (`centreType` required for the centre role); the widened public directory (`/api/hospitals` route and `/encuentra-tu-momento` slug — names UNCHANGED, per the naming constraint above); the five new seeded centre types; any new integration test requiring `VIVETUTIEMPO_RUN_INTEGRATION=true` (`centre-migration.test.ts`, `centre-lifecycle.test.ts`) and the "must re-seed after any integration run" gotcha (already documented from the prior change — confirm it still reads correctly, extend if needed).
- [ ] 7.2 `docs/memoria-tfm-borrador.md` (edit): rewrite the product description from hospital-only to the six-kind generalised model; document the D16–D20 architectural decisions as the thesis-gradable claim ("role and kind are two orthogonal axes; the seventh centre type is data, not code"); reference the migration-safety verification (Phase 1.9) as evidence.
- [ ] 7.3 `docs/slides-outline.md` (edit): update any slide describing the product/demo scope to the six-kind model; add a slide or bullet on the D16 role/kind separation as the gradable architectural claim.
- [ ] 7.4 `docs/security-threat-model.md` (edit): add the widened safeguarding risk as an **explicit, accepted, demo-scoped open risk** — widening onboarding to residencias, disability day/occupational centres and palliative units raises the safeguarding bar (populations with cognitive impairment/vulnerable adults; weaker independent trust markers than large public hospitals; single-surface identifiability for a small/rare kind, e.g. a lone palliative unit in a city); verification stays self-declared with admin validation, unchanged in shape; real institutional verification/accreditation named as the next follow-on. No claim beyond the demo framing (proposal §4.3/§4.4-7) — do not assert a safeguarding posture the code does not implement.
- [ ] 7.5 `docs/tfm-readiness-report.md` (edit): update the readiness assessment to reflect the widened model, the migration verification evidence (Phase 1.9/1.10), the six-type integration lifecycle test (Phase 2.9), and the Basque review gate (Phase 6.10) as an explicitly OPEN item, not closed.
- [ ] 7.6 Cross-check: confirm no doc edited in this phase overstates verification/safeguarding beyond what Phase 7.4 records — read 7.1–7.5 together before considering this phase done.

## Phase 8: Full Verification Gate — non-TDD, final

- [ ] 8.1 Run `npm run test` (unit) — confirm green, no regression against the baseline from `hospital-finder-and-home-clarity`.
- [ ] 8.2 Run `VIVETUTIEMPO_RUN_INTEGRATION=true npm run test` — confirm every integration test in this change (`centre-migration.test.ts`, `centre-lifecycle.test.ts`, extended `public-hospital-directory-query.test.ts`) reports as run and passed, never skipped. **Immediately re-run `npm run db:seed`** before any subsequent e2e or manual step.
- [ ] 8.3 Run `npm run test:e2e` — confirm `hospital-directory.spec.ts`, `non-correlation.spec.ts`, and any pre-existing spec touching registration/admin validation are green. Root-cause and document (do not hand-wave) any failure in a file unrelated to this change's scope, per the precedent set in the prior change's own verification gate.
- [ ] 8.4 Run `npx tsc --noEmit` and `npm run lint` — confirm clean across the whole repo, not just touched files (the enum rename is type-level and can surface a stale literal anywhere).
- [ ] 8.5 Confirm Phase 6.10 (Basque native-speaker review) is explicitly reported as OPEN/PENDING if not yet resolved — this gate does NOT wait on it to run the rest of the suite, but the change is not merge-ready without it.
- [ ] 8.6 Confirm the D4 partial-unique-index behaviour (SUBMITTED/ACCEPTED singleton per slot) is unaffected — re-read 1.8/1.9's evidence rather than re-deriving it here.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Total tasks | 8 phases (0 pre-flight, 1–7 delivery, 8 final gate), ~55 checklist items |
| Estimated changed lines | ~2,660 total across 7 PRs (see per-PR table) — this is CHANGED lines, distinct from the ~2,650 raw `hospital` *occurrences* in the repo today, most of which (internal identifiers kept per §5.4) require NO edit |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Chain strategy | `feature-branch-chain` (received this run — already resolved, not re-asked) |
| Delivery strategy | `ask-on-risk` (received this run) |
| Decision needed before apply | No (resolved — chain strategy already given; proceed with the 7-PR chain below) |

### Per-PR Line Estimate

| PR | Scope | Phase(s) | Est. lines | Depends on | Risk |
|----|-------|----------|-----------:|------------|------|
| PR1 | Domain model + hand-written migration (D16, D17): both enum renames, `CentreType` axis, mappers, migration SQL, migration-survival integration test, rollback verification | 1 | ~465 | Phase 0 state check only | **Highest — hand-written raw SQL migration, real-Postgres data-loss risk** |
| PR2 | Registration redesign + guards (D18): `registerProfile`, route, register UI, admin queue display, ~8 guard call-site literal swaps, six-type lifecycle integration test | 2 | ~570 | PR1 | Medium-high (widest call-site surface) |
| PR3 | Public directory application layer (D19a): DTO/allow-list, use-case rebuild, D10 non-correlation re-run both directions | 3 | ~185 | PR2 | Medium (security-predicate-adjacent, but small diff by design) |
| PR4 | Public directory infrastructure + seed diversification (D19b): adapter, integration test extension, 5 new-kind seed rows | 4 | ~280 | PR3 | Medium |
| PR5 | Client-side filter + Finder UI + e2e (D12 extension, D19 UI, D10 e2e re-run) | 5 | ~400 | PR4 | Low-medium |
| PR6 | Vocabulary, three languages (D20): mechanical labels then narrative rewrite, locale parity extension, manual review tasks, Basque blocking gate | 6 | ~360 | PR5 | Medium (judgement-heavy, but text-only — no runtime/security risk) |
| PR7 | TFM documentation (5 files; `video-script.md` excluded) | 7 | ~410 | PR6 | Low (text-only) |
| — | Final verification gate | 8 | 0 (verification only) | PR7 | — |

**Total estimated ~2,660 changed lines across 7 PRs**, none individually near the 400-line budget except PR2 (~570, flagged) — if PR2 proves larger in practice, consider splitting it into 2a (registration use case + route + integration test) and 2b (guards + registration UI + admin queue display) before opening the PR. This forecast is honest about scale: this is the largest change since `bootstrap-vivetutiempo-platform` itself, carrying a real data migration, a full three-language copy rewrite, and a public-surface allow-list widening in the same dependency chain.
