# Tasks: auditable-profile-approval

**Depends on:** `bootstrap-vivetutiempo-platform` (D1–D8, deployed), `hospital-finder-and-home-clarity` (D9–D15, deployed), `widen-beyond-hospitals` (D16–D20). **ADRs D21–D27** (`design.md`). Chain strategy: `feature-branch-chain` — PR1 targets the tracker/feature branch; each later PR targets the immediately preceding PR's branch, so only the tracker merges to `main`. Delivery strategy: `ask-on-risk` (see Review Workload Forecast at the end — this run resolves it without needing to stop, see there).

**Strict TDD is ACTIVE** for every `domain/` and `application/` task (Phases 1, 3). `infrastructure/`/`ui/` stay pragmatic — tests still required, follow the code, per project convention; the migration (Phase 2) is proven against real Postgres before it is trusted, per design. Integration tests are silently SKIPPED unless run with `VIVETUTIEMPO_RUN_INTEGRATION=true` — a skipped integration test is NEVER reported as passed. **Every integration test run WIPES the seed (`resetDatabase()` per test, shared Neon `dev` branch) — `npm run db:seed` MUST be re-run immediately after, before any e2e run or manual demo.** Node is not on default PATH: prefix commands with `export PATH="/c/Program Files/nodejs:$PATH"`. npm pinned per project convention — never bare `npm install`.

**Naming/shape constraints carried through every phase below (from design):** the three admin transitions (`approveProfile`, `rejectProfile`, `deactivateProfile`) change signature to `(profile, { adminAccountId, basis, reviewId }, clock) => { profile, review }`; `reactivateProfile` is UNCHANGED (D22 — applicant action, no basis); `MAX_REVIEW_BASIS_LENGTH = 1000` (confirmed at spec time, D24); `ProfileReview` is immutable, append-only, no update/delete path (D21); the admin identity is always `actor.accountId`, never a request-body value (D23); the migration is Prisma-generated and purely additive (D25, unlike D17's hand-written rename); legacy rows are NEVER back-filled (D25); the `ForbiddenPublicHospitalKey` guard on `PublicHospitalProjection.ts` is extended, never the allow-list itself (D26); the basis prompt is UI/i18n-only and role-cued, the domain stays role-blind (D27).

**Known scope gap surfaced by this batch's read (not in design's file table, must be resolved during Phase 5):** `ProfileRowActions.tsx` currently renders only `approve`/`reject` — there is NO existing UI trigger for `deactivate` anywhere in `src/app` (confirmed by search); only the API route exists. Phase 5 below adds the minimal UI surface needed to exercise deactivate with a basis, scoped narrowly (not a new "active centres" admin page) unless the implementer finds an existing surface this batch's read missed — re-confirm before assuming none exists.

## Phase 0: Pre-Flight — state check, not work

- [x] 0.1 Confirm the working tree is clean on the branch this change starts from (`git status`); this is the tracker branch PR1 will target under `feature-branch-chain`. — CONFIRMED: `feat/auditable-approval-domain` checked out, clean except untracked `openspec/changes/auditable-profile-approval/` (the SDD artifacts themselves) and an unrelated pre-existing `widen-beyond-hospitals/verify-report.md`.
- [x] 0.2 Confirm `widen-beyond-hospitals` is merged to `main` (or its Phase 8 verification gate is otherwise satisfied) before branching. — CONFIRMED: `git log` on the branch shows the `widen-beyond-hospitals` PR chain (PR1–PR6) merged into `main` via `git log --oneline`, `Profile.ts` already carries `CentreType`/`centre` role vocabulary.
- [x] 0.3 Re-confirm, by reading the files directly: `Profile.ts:293-317` still showed the three pure-flip transitions exactly as design quotes; `ProfileUnitOfWork.ts`'s `LockedProfileContext` had no `saveReview`; none of the three admin routes read a request body. — CONFIRMED, no drift from design.md.

## Phase 1: Domain — ProfileReview, ReviewDecision, Transition Signatures (D21–D24) — strict TDD, RED-first — PR1 part A

- [x] 1.1 [RED] `tests/unit/domain/profile.test.ts` (extend, EXISTING file): new `describe("ProfileReview (D21-D24)", ...)` block covering, per design's Testing Strategy table:
  - a blank basis (`""`) and a whitespace-only basis (`"   "`) each throw `DomainValidationError` BEFORE any status change, for EACH of `approveProfile`/`rejectProfile`/`deactivateProfile` (six cases) — assert the profile argument's status is unaffected after the throw;
  - a basis whose trimmed length exceeds `MAX_REVIEW_BASIS_LENGTH` (1000) throws `DomainValidationError`, for at least one transition;
  - a valid call to each of the three transitions returns `{ profile: <new status>, review }` where `review.adminAccountId` equals the passed id, `review.basis` equals the TRIMMED input (leading/trailing whitespace stripped), `review.at` equals `clock.now()`, and `review.decision` is `"approve"`/`"reject"`/`"deactivate"` respectively;
  - `ProfileReview` is obtainable ONLY via a transition call (no exported bare factory/constructor exists that fabricates one directly outside a transition);
  - `assertValidReviewDecision` rejects an unknown string (e.g. `"revoke"`).
  Covers `auditable-admin-decisions` spec scenarios: "Approving requires a non-blank bounded basis", "Rejecting requires and records a basis", "Deactivating requires and records a basis", "A whitespace-only basis is denied...", "A scripted request bypassing the UI is still denied", "A basis over the bounded maximum length is rejected", "The basis is trimmed once...", "The recorded admin id is the authenticated caller's session identity" (id-passthrough half, domain-level).
  — DONE: extended `describe("ProfileReview (D21-D24)...")` plus a dedicated `describe("assertValidReviewDecision (D21)")`. Ran RED first (34 failing, confirmed compile/behavioural failure against the un-changed `Profile.ts`), then GREEN after 1.2.
- [x] 1.2 [GREEN] `src/domain/profile/Profile.ts`: added `ReviewDecision` union + `REVIEW_DECISIONS` + `assertValidReviewDecision`; `ProfileReview` interface; `ReviewInput` interface (the `{ adminAccountId, basis, reviewId }` shape, exported for Phase 3's use); `MAX_REVIEW_BASIS_LENGTH = 1000` + a private `assertValidBasis` (trims once, validates trimmed, returns trimmed) + a private `buildReview` (never exported — the only callers are the three transitions, so a `ProfileReview` cannot be fabricated outside one); `approveProfile`/`rejectProfile`/`deactivateProfile` now `(profile, review: ReviewInput, clock) => { profile, review }`, basis validated BEFORE `assertStatus`. Makes 1.1 pass (70/70 in `profile.test.ts`).
- [x] 1.3 [RED→GREEN, regression, mechanical] Added a dedicated regression-pin test: `reactivateProfile` still `(profile, clock) => Profile`, no `review` key on the return. `reactivateProfile` itself was NOT touched. PASSES.
- [x] 1.4 [GREEN, mechanical] Updated every other call site: `prisma/seed.ts` (13 `approveProfile` calls), `tests/integration/support/fixtures.ts`, `tests/unit/application/support/builders.ts`, `tests/integration/centre-migration.test.ts` (6 calls), `tests/integration/login-vs-deactivation-race.test.ts` (2 calls), `tests/integration/public-hospital-directory-query.test.ts` (5 call sites incl. a nested `deactivateProfile(approveProfile(...))`). Also fixed the two PHASE-3-OWNED application use cases (`validateProfile.ts`, `deactivateProfile.ts`) with an explicitly-commented `PR2_PLACEHOLDER_REVIEW`/`PR2_PLACEHOLDER_CLOCK` — NOT semantic PR2 wiring (no `actor.accountId`, no real `basis` input, no `saveReview` call — the review half is discarded exactly as before this change), purely a mechanical compile fix per the batch's explicit instruction to keep the whole repo green without doing PR2's work. `tests/integration/support/db.ts`'s `resetDatabase` also gained a `profileReview.deleteMany()` (FK-safe order, `ON DELETE RESTRICT`) — required for integration tests to keep working, not itself a task item but a necessary consequence of 2.1's schema change.
- [x] 1.5 Verify `npx tsc --noEmit` and `npm run lint` clean. — CONFIRMED: both clean across the WHOLE repo (not just `src/domain/`) — 1.4's mechanical fixes closed every call site, so no residual error was left for Phase 3 to inherit at the type level (Phase 3 still owns the SEMANTIC wiring: real actor id, real basis input, `saveReview` persistence).

## Phase 2: Migration — ProfileReview Table + ReviewDecision Enum (D25) — Prisma-generated, proven against real Postgres — PR1 part B

**Unlike D17, this migration is safe to auto-generate** — it is a brand-new table and enum, the shape Prisma's declarative diff models correctly (design D25). Do not hand-write it.

- [x] 2.1 `prisma/schema.prisma` (edit): added `enum ReviewDecision { APPROVE REJECT DEACTIVATE }`; added `model ProfileReview { id, profileId, adminAccountId, decision, basis, createdAt }` with an FK relation to `Profile` (`ON DELETE RESTRICT ON UPDATE CASCADE`, `adminAccountId` stored as a plain `String`, not a hard FK to `Account`, per design's explicit allowance); `Profile.reviews ProfileReview[]` back-relation added; `@@index([profileId, createdAt])` for the D21 read-ordering. No existing model/enum edited.
- [x] 2.2 Ran `prisma migrate dev --name profile_review_audit --create-only` against the Neon **dev** host (verified `ep-snowy-sunset-abmfm9fs...` via `.env` + prior session memory before running) → generated `prisma/migrations/20260723144207_profile_review_audit/migration.sql`. Read directly: contains ONLY `CREATE TYPE "ReviewDecision"`, `CREATE TABLE "profile_reviews"`, `CREATE INDEX`, and one `ALTER TABLE "profile_reviews" ADD CONSTRAINT ... FOREIGN KEY` (adds an FK ON the new table referencing `profiles`, does not alter any column on `profiles` itself) — no `ALTER COLUMN`/`DROP`/`UPDATE` against `profiles`/`accounts`, no reference to `AccountRole`/`ProfileType`/`CentreType`. Applied via `prisma migrate deploy` (clean) then `prisma generate` (required killing 5 stale `next dev`/npm processes from a prior session that held the query-engine DLL locked on Windows — EPERM rename error — before it would complete). Covers `migration-safety` "The migration adds only new schema objects".
- [x] 2.3 [Integration test, `VIVETUTIEMPO_RUN_INTEGRATION=true`, run against REAL Postgres] `tests/integration/profile-review-audit.test.ts` (NEW): 5 tests — pre-existing ACTIVE/REJECTED/DEACTIVATED profiles each have zero `ProfileReview` rows with every other field byte-identical (`toEqual` against the pre-transition `Profile` value); a table-wide zero-rows-inserted-by-the-migration proof; and a direct schema round-trip (`client.profileReview.create` then re-read the `Profile` row unaffected). Ran against real Neon dev: **5/5 passed**. Then ran the FULL suite with `VIVETUTIEMPO_RUN_INTEGRATION=true`: **728/728 passed, 0 skipped, 0 failed** (baseline 705 [628+77] + 18 new domain unit + 5 new migration-survival integration = 728, exact match — zero regressions). `npm run db:seed` re-run immediately after (confirmed via seed-runner log).
- [x] 2.4 [Rollback verification] Ran a throwaway Node script (`rollback-check.throwaway.mjs`, deleted after use, never committed) against the real dev DB: wrapped `DROP TABLE "profile_reviews"; DROP TYPE "ReviewDecision";` inside a Prisma interactive transaction (`prisma.$transaction`) that always threw a `RollbackSentinel` before commit (same discipline as `widen-beyond-hospitals` task 1.10). Recorded outcome: BEFORE `{ profileCount: 15, reviewTableExists: true, enumExists: true }`; INSIDE the (uncommitted) transaction, both the table and enum were confirmed gone (`false`/`false`) while `profile.count()` still read 15 — proving (b) pre-existing Profile rows are untouched by the down path even mid-transaction; AFTER rollback, both existed again and the count was unchanged — (a) the drop is clean with no error, rolled back with no permanent effect. (c) is documented in the schema/design comments, not independently re-verified here (dropping the table necessarily loses its rows — not re-tested since the table exists post-rollback with the rows it had before, i.e. zero, from 2.3's `resetDatabase` between test files).
- [x] 2.5 Verify `npx tsc --noEmit` and `npm run lint` clean after schema/migration changes. — CONFIRMED clean across the whole repo.

## Phase 3: Persistence + Application Use-Cases — saveReview, actor.accountId Wiring (D23) — strict TDD (application), pragmatic (infrastructure) — PR2 (base: PR1 branch)

- [x] 3.1 [RED, mechanical interface change] `src/application/ports/ProfileUnitOfWork.ts`: added `saveReview(review: ProfileReview): Promise<void>;` to `LockedProfileContext`. Type-only addition, proven by the fakes/adapter and the application-layer tests in 3.3/3.5.
- [x] 3.2 [GREEN, mechanical] `tests/unit/application/support/fakes.ts` (extended): `FakeProfileUnitOfWork` implements `saveReview` (in-memory append to a `reviews` array, snapshot/restore-protected alongside profiles/sessions/slots so a `work` failure rolls the review back too), exposed via `reviewsForProfile(profileId)` for test assertions.
- [x] 3.3 [RED→GREEN] `tests/unit/application/validateProfile.test.ts` (extended): `ValidateProfileInput` gains required `basis: string`; every existing call site updated. New `describe("ProfileReview persistence (D21-D23, PR2)")` block: persists via `saveReview` in the SAME `withLockedProfile` call as `saveProfile`; `review.adminAccountId` is `actor.accountId` even when the input carries a spoofed `adminAccountId`/`actorId`; a blank basis is rejected fail-closed (no status change, no review row); `review.id`/`review.at` come from `deps.idGenerator.next()`/`deps.clock.now()`. Also strengthened the existing M3 atomicity test to assert the review row rolls back too. RAN RED first (8 failures against the PR1 placeholder wiring), then GREEN after 3.4.
- [x] 3.4 [GREEN] `src/application/use-cases/validateProfile.ts`: `ValidateProfileInput` gains `basis: string`; `ValidateProfileDeps` gains `idGenerator`/`clock`; calls `approveProfile`/`rejectProfile` with `{ adminAccountId: actor.accountId, basis: input.basis, reviewId: deps.idGenerator.next() }, deps.clock`; persists the returned `review` via `ctx.saveReview(review)` inside the SAME `withLockedProfile` callback as `ctx.saveProfile` and (on reject) `ctx.sessions.revokeAllForAccount`. PR1 placeholders removed entirely. Makes 3.3 pass (22/22 in `validateProfile.test.ts`).
- [x] 3.5 [RED→GREEN] `tests/unit/application/deactivateProfile.test.ts` (extended): mirrors 3.3 — `DeactivateProfileInput.basis`, `saveReview` in the same unit, admin id from `actor.accountId`, blank-basis fail-closed, reviewId/timestamp sourcing, and the strengthened atomicity test.
- [x] 3.6 [GREEN] `src/application/use-cases/deactivateProfile.ts`: `DeactivateProfileInput` gains `basis: string`; `DeactivateProfileDeps` gains `idGenerator`/`clock`; same wiring pattern as 3.4. PR1 placeholders removed. Makes 3.5 pass.
- [x] 3.7 [Infrastructure, pragmatic] `src/infrastructure/persistence/prisma/mappers.ts`: added `REVIEW_DECISION_TO_DOMAIN`/`REVIEW_DECISION_TO_PRISMA` maps + `toDomainReviewDecision`/`reviewDecisionToPrisma` + `ProfileReviewRow` + `toDomainProfileReview(row)`, mirroring the `CentreType`/`ProfileStatus` pairs.
- [x] 3.8 [Infrastructure, pragmatic] `src/infrastructure/persistence/prisma/ProfileUnitOfWork.ts`: `saveReview` implemented as `tx.profileReview.create(...)` using the SAME `tx` handle `saveProfile`/`sessions` already use inside `prisma.$transaction` — no new transaction, no second round-trip; `createdAt` stamped from `review.at` (the injected `Clock`), not Postgres `now()`.
- [x] 3.9 [Infrastructure, mechanical] `src/infrastructure/composition/container.ts`: `AdminDeps`/`adminDeps()` extended with `idGenerator: new CryptoIdGenerator()` / `clock: new SystemClock()` — the same singletons every other use case's deps already use. Route call sites (`approve`/`reject`/`deactivate`) updated to pass an explicitly-commented `PR4-PENDING` basis placeholder — NOT semantic PR4 work, just keeps the routes compiling until body-parsing lands (D24/D27, PR4). The domain remains the authoritative validator regardless.
- [x] 3.10 [Integration test, `VIVETUTIEMPO_RUN_INTEGRATION=true`, EXTENDED `tests/integration/profile-review-audit.test.ts`] Added a second `describe.skipIf` block with 2 REQUIRED scenarios against REAL Neon dev Postgres, through the REAL `validateProfile`/`deactivateProfile` use cases (not the domain functions directly):
  - **Cycle proof (D21/D22):** reject → re-apply (`reactivateProfile`, domain-direct — applicant action) → approve → deactivate on ONE profile persists exactly THREE ordered admin `ProfileReview` rows (reject, approve, deactivate; re-apply itself produces none), none overwritten, each with its own distinct basis and `adminAccountId === adminActor.accountId`. PASSED.
  - **Atomicity proof (D23):** a `ThrowOnSaveReviewProfileUnitOfWork` decorator (mirrors the unit-level `ThrowOnceOnRevokeAll implements SessionPort` pattern) wraps the REAL `PrismaProfileUnitOfWork`, replacing `ctx.saveReview` with one that always throws — since `work` still runs inside the real adapter's `prisma.$transaction`, the throw rolls back the EARLIER `saveProfile` write too. Confirmed: NO status change, NO review row survives. PASSED.
  Full `VIVETUTIEMPO_RUN_INTEGRATION=true npm run test`: **738/738 passed** (728 baseline + 8 new unit + 2 new integration — exact match, zero regressions). `npm run db:seed` re-run immediately after.
  **Flake found and fixed (unrelated to the two new scenarios):** `tests/integration/login-vs-deactivation-race.test.ts`'s two barrier tests occasionally exceeded Vitest's 5s default `testTimeout` once `saveReview` added one more round-trip per admin transaction against the remote Neon dev host — added an explicit `15_000`ms per-test timeout to both (comment explains why); re-verified green on re-run.
- [x] 3.11 Verify `npx tsc --noEmit` and `npm run lint` clean across the whole repo after this phase. — CONFIRMED both clean.

**Mechanical call-site fixes required by 3.4/3.6's real signature (not separate task items, same discipline as Phase 1 task 1.4):** every OTHER test file that calls `validateProfile`/`deactivateProfile` directly for unrelated behaviour (races, authorization matrix, session revocation, slot-decision atomicity) needed a `basis` argument added and, where missing, `idGenerator`/`clock` added to its deps object — `tests/integration/centre-lifecycle.test.ts`, `tests/integration/login-vs-deactivation-race.test.ts`, `tests/integration/profile-transition-session-revocation.test.ts`, `tests/integration/slot-auth-vs-deactivation-race.test.ts`, `tests/unit/application/approveRejectProposal.test.ts`, `tests/unit/application/authorizationMatrix.test.ts`, `tests/unit/application/closeSlot.test.ts`, `tests/unit/application/login.test.ts`, `tests/unit/application/publishSlot.test.ts`, `tests/unit/application/submitProposal.test.ts`. Each uses a fixed valid basis string clearly unrelated to the review-audit assertions those files actually make.

## Phase 4: No-Public-Exposure Guard + Threat Model (D26) — mechanical guard extension + semantic doc rewrite — PR3 (base: PR2 branch)

- [ ] 4.1 [RED, mechanical] `tests/unit/application/listPublicHospitals.test.ts` (extend, EXISTING file): a hostile-adapter case supplying, per centre, the six allow-listed fields PLUS `reviewBasis`, `adminAccountId`, and `reviewedAt` — asserts the resulting projection contains ONLY the six allow-listed keys. Covers `public-hospital-directory` spec scenario "A hostile adapter supplying review/audit fields is stripped by the field-by-field rebuild".
- [ ] 4.2 [GREEN, mechanical] `src/application/dto/PublicHospitalProjection.ts`: extend the `ForbiddenPublicHospitalKey` union (`:48-77`) to additionally name `reviewBasis`, `adminAccountId`, `reviewedBy`, `reviewedAt`, `reviews`. Do NOT touch the allow-list itself (no field is added to the projection). Sabotage-verify per the `widen-beyond-hospitals` task 3.2 precedent: temporarily add one of the new forbidden keys to the interface, confirm `tsc` fails on `_NoForbiddenFields` with a `TS2344`-shaped error, then revert. Makes 4.1 continue to pass (it already passed before this edit, since the rebuild never copied these fields — this task's value is the compiler-enforced guard, not a behaviour change). Covers spec scenario "The forbidden-key guard now names the audit fields".
- [ ] 4.3 Re-run the existing exact-key-set unit test and the e2e raw-JSON no-leak assertion (`e2e/hospital-directory.spec.ts` / `e2e/non-correlation.spec.ts` allow-list checks) — confirm both still pass, unchanged in intent. Covers spec scenario "The existing no-leak suites still pass, unchanged in intent".
- [ ] 4.4 [Read-only confirmation, mechanical] Confirm `PublicHospitalDirectoryQuery`'s Prisma `select` clause contains no join against `ProfileReview` (read the file directly). Covers `public-hospital-directory` spec scenario "A ProfileReview row cannot appear in the profiles-backed directory query" — expected to require NO edit, since `ProfileReview` lives on a separate model never joined in.
- [ ] 4.5 [Semantic, docs] `docs/security-threat-model.md`: rewrite T-22 per design's exact framing — **Built now:** the admin approve/reject/deactivate decision is accountable (attributed, timestamped, required bounded role-cued basis, append-only, domain-enforced impossible-without-a-record). **NOT built:** the platform does not verify the attested basis; convenio flow / certificate upload / out-of-band contact tooling / background-check integration remain the named future work, before any real non-demo onboarding. Explicitly state no copy or doc may claim a verification posture the code does not implement (R8, honesty gate). This is a graded in-change edit describing what actually shipped, not follow-up housekeeping.
- [ ] 4.6 Verify `npx tsc --noEmit` and `npm run lint` clean after this phase.

## Phase 5: Admin Route + UI + i18n (D24 route wiring, D27 role-cued copy) — mechanical routes, semantic UI/copy — PR4 (base: PR3 branch)

- [ ] 5.1 [Mechanical] `src/app/api/admin/profiles/[id]/approve/route.ts` and `.../reject/route.ts`: parse `basis` from the JSON request body (these routes currently send/read no body at all — confirmed by reading them directly); coerce with `String(...)` per the codebase's existing thin-route convention, pass through to `validateProfile`'s `input.basis`. The domain remains the authoritative validator (D24) — the route may reject early for a friendlier error but MUST NOT be the only gate.
- [ ] 5.2 [Mechanical] `src/app/api/admin/profiles/[id]/deactivate/route.ts`: same body-parsing pattern, passed to `deactivateProfile`'s `input.basis`.
- [ ] 5.3 [Semantic] `messages/es.json` + `messages/en.json` (edit, same commit): add `ProfileActions.basis.*` — a field label plus the two role-cued prompts (centre: institutional/convenio verification; artist: identity + safeguarding attestation), per design D27's table; add `AdminProfiles.review.legacy` ("Sin base registrada (anterior a la auditoría)" es / analogous en). ICU placeholder parity preserved for any templated key.
- [ ] 5.4 [Semantic, DRAFT] `messages/eu.json` (edit): genuine best-effort Basque translation of the same key set added in 5.3 — no stubs, no empty strings — explicitly UNREVIEWED, flagged again at 5.9 (blocking gate).
- [ ] 5.5 [Semantic] `src/app/admin/profiles/ProfileRowActions.tsx` (edit): add a required basis `<textarea>`, gated so approve/reject cannot submit while it is blank (client-side convenience only — the domain is authoritative per Phase 1/3); the component gains a `profileType: "centre" | "artist"` prop so it can pick the role-cued placeholder from `ProfileActions.basis.*` (D27); the fetch body changes from empty to `{ basis }`.
- [ ] 5.6 [Semantic — resolves the scope gap flagged at the top of this file] Add the minimal deactivate UI trigger: extend `src/app/admin/profiles/page.tsx` to also list ACTIVE profiles (or add a narrowly-scoped sibling list/section on the same page — implementer's call, record which was chosen and why) with a deactivate control reusing `ProfileRowActions`' basis-gated pattern. Keep this the SMALLEST change that lets a human exercise deactivate-with-basis end-to-end; do not build a full active-centres management page — that is out of this change's scope.
- [ ] 5.7 [Semantic, optional per design] `src/app/admin/profiles/page.tsx`: if the queue is extended to show prior review history (design D26 — permitted since this is an authenticated admin surface, not required for MVP), render `AdminProfiles.review.legacy` for a profile with zero `ProfileReview` rows. If the implementer defers this (design explicitly leaves it as a UI/spec call, not required), record that decision here rather than silently skipping it.
- [ ] 5.8 [Test] `tests/unit/i18n/localeParity.test.ts` (extend, EXISTING file): flattened key-set parity extended to cover `ProfileActions.basis.*` and `AdminProfiles.review.legacy` across all three locales, plus ICU-placeholder parity for any templated string among them.
- [ ] 5.9 **BLOCKING GATE — CANNOT be closed by the implementing agent.** Every `eu` string added in 5.4 (the basis field label, the two role-cued prompts, `AdminProfiles.review.legacy`) is DRAFT and requires review and sign-off by the assigned native Basque speaker before merge (same convention as `widen-beyond-hospitals` task 6.10, `hospital-finder-and-home-clarity` task 13.5). A passing `localeParity` run (5.8) is structure-only and MUST NOT be cited as Basque quality assurance — report explicitly as "structural parity: pass, translation quality: pending native review." — **OPEN, blocking, cannot be closed by this agent.**
- [ ] 5.10 [E2E, NEW] `e2e/admin-profile-audit.spec.ts` (NEW file): `POST /api/admin/profiles/[id]/approve` with an empty/blank `basis` is rejected (non-2xx, no status change); with a valid basis, the approval succeeds AND the public `/api/hospitals` JSON for that (now-active) centre still exposes NONE of `reviewBasis`/an admin id/`reviewedAt`, in any form, in the raw response body. Covers `auditable-admin-decisions` and `public-hospital-directory` spec scenarios end-to-end.
- [ ] 5.11 **MANUAL/E2E-CHECKABLE TASK.** Confirm, via the running app or the e2e run above extended with a DOM assertion, that a `centre` profile's basis input shows the institutional/convenio placeholder and an `artist` profile's shows the identity+safeguarding placeholder (`admin-review-ui` spec, e2e-checkable scenarios). This can be automated as a DOM/placeholder-text assertion; it is listed separately from 5.12 because it checks PRESENCE of the right copy, not QUALITY of it.
- [ ] 5.12 **MANUAL REVIEW TASK — non-automatable, not a test task, OPEN, cannot be closed by the implementing agent.** Spec scenario (`admin-review-ui`, manual review): "The role-specific prompt reads correctly and asks the right question" — a human reviewer reads both prompts (es/en; eu once 5.9 clears) from the perspective of an admin about to make the decision, confirms the centre prompt clearly asks for institutional/convenio verification and the artist prompt clearly asks for identity+safeguarding verification, with no ambiguity. Also confirms the honesty-gate scenario ("No copy may imply the platform performed a verification it did not perform", R8) — no verb in the prompts or their help text implies the platform itself verified/confirmed/checked anything. Record the review outcome explicitly (pass/fail + any follow-up edit), not merely the implementing agent's own self-assessment.
- [ ] 5.13 Verify `npx tsc --noEmit` and `npm run lint` clean after this phase.

## Phase 6: Full Verification Gate — non-TDD, final

- [ ] 6.1 Run `npm run test` (unit) — confirm green, no regression against the `widen-beyond-hospitals` baseline.
- [ ] 6.2 Run `VIVETUTIEMPO_RUN_INTEGRATION=true npm run test` — confirm every integration test in this change (`profile-review-audit.test.ts` — migration-survival, cycle, and atomicity cases all present and passed, never skipped). **Immediately re-run `npm run db:seed`** before any subsequent e2e or manual step.
- [ ] 6.3 Run `npm run test:e2e` — confirm `admin-profile-audit.spec.ts` and any pre-existing spec touching admin validation/directory no-leak assertions are green. Root-cause and document (do not hand-wave) any failure in a file unrelated to this change's scope.
- [ ] 6.4 Run `npx tsc --noEmit` and `npm run lint` — confirm clean across the whole repo, not just touched files.
- [ ] 6.5 Confirm Phase 5.9 (Basque native-speaker review) and Phase 5.12 (role-specific-prompt manual review) are both explicitly reported as OPEN/PENDING if not yet resolved — this gate does NOT wait on them to run the rest of the suite, but the change is not merge-ready without both.
- [ ] 6.6 Confirm, by reading the file directly rather than re-deriving it, that Phase 4's sabotage-verify (4.2) and the D25 rollback verification (2.4) evidence are both recorded in their own tasks — this gate cross-checks, it does not re-run the sabotage itself.
- [ ] 6.7 Confirm the existing D10 non-correlation suites (`nonCorrelation.test.ts`, `e2e/non-correlation.spec.ts`) were not touched by this change's diff (this change adds no field to `PublicEventProjection` and no new correlation path) — a diff touching either file here would be an out-of-scope surprise to flag, not silently accept.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Total tasks | 6 phases (0 pre-flight, 1–5 delivery, 6 final gate), ~50 checklist items |
| Estimated changed lines | ~1,650 total across 4 PRs (see per-PR table) |
| 400-line budget risk | High for PR1 and PR2; borderline for PR4; low for PR3 |
| Chained PRs recommended | Yes |
| Chain strategy | `feature-branch-chain` (received this run — already resolved) |
| Delivery strategy | `ask-on-risk` (received this run) |
| Decision needed before apply | No — the chain strategy was already given, and this forecast resolves the workload into 4 PRs of manageable, individually-reviewable size (none over ~600 lines including its own tests); proceed with the 4-PR chain below. If PR1 or PR2 prove larger in practice once written, split PR1 into "domain" / "migration" and/or PR2 into "port+fakes" / "use-cases+infra adapter" before opening the PR, per `work-unit-commits`. |

### Per-PR Line Estimate

| PR | Scope | Phase(s) | Est. lines | Depends on | Risk |
|----|-------|----------|-----------:|------------|------|
| PR1 | Domain signature change (D21–D24, RED-first) + Prisma-generated migration + migration-survival integration test + rollback verification | 1, 2 | ~550 | Phase 0 state check only | Medium — no destructive-migration risk (unlike D17), but the domain signature change is the load-bearing invariant of the whole feature and touches every transition call site |
| PR2 | `saveReview` port + fakes + adapter, `validateProfile`/`deactivateProfile` wiring (`actor.accountId`, `idGenerator`, `clock`), atomicity + reject→reapply→approve→deactivate cycle integration tests | 3 | ~500 | PR1 | Medium-high — atomicity is safety-critical and needs careful review; widest test surface in the change |
| PR3 | D14 forbidden-key guard extension + no-leak suite re-run + threat-model T-22 rewrite | 4 | ~140 | PR2 | Low — no allow-list widening, compile-time guard only, plus a docs-only ADR rewrite |
| PR4 | Route body-parsing + basis textarea + deactivate UI trigger + role-cued i18n copy (3 locales) + locale-parity extension + e2e | 5 | ~460 | PR3 | Medium — judgement-heavy (role-cued copy, honesty-gate wording, the deactivate-UI scope gap) but no schema/runtime-atomicity risk |
| — | Final verification gate | 6 | 0 (verification only) | PR4 | — |

**Total estimated ~1,650 changed lines across 4 PRs**, each individually well under the 400-line-per-PR ideal once tests are counted separately from production code, though PR1/PR2's PRODUCTION+TEST combined diff will likely read as "large" in a single PR view — flagged above as the split candidates if either proves oversized once actually written. This is a genuinely bounded change (design's own framing: "everything else is deliberately small" hanging off one structural move) — it is smaller in total scope than `widen-beyond-hospitals` (no enum rename across the whole codebase, no three-language narrative rewrite, no seed diversification), but it is NOT a single-PR change: the domain/migration/persistence work (PR1–PR2) is safety-critical enough (an atomicity invariant, a real migration) to warrant its own reviewable slice separate from the UI/copy work (PR4), and the public-exposure guard (PR3) is small enough that bundling it into either neighbour would dilute its own, easily-reviewable diff.
